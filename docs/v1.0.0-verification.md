# v1.0.0 verification

Released: 2026-09-24T16:57:28Z
Commit:   737ac0973363f52693a21682a1b53be45b3b0630
Tag:      v1.0.0

## Gates

Local (all pass):

- tools/validate.py
- tools/check_version_consistency.py
- tools/check_snapshot_freshness.py
- tools/export_csv.py --check
- tools/export_sql.py --check
- tools/export_parquet.py --check
- tools/sync_wrappers.py --check
- pytest tests/ — 66 pass
- tools/check_cross_language.sh — 5 suites pass

CI (`validate` workflow, all jobs pass):

- validate-json
- check-exports
- check-cli
- check-wrappers (python / javascript / rust / go)
- check-cross-language

## Known warnings

- [cross-ref] 3 MICs referenced by Exchange Calendar not in registry:
  XBEK, XNBO, XQSE. Documented in docs/PROVENANCE.md § Known
  cross-registry gap. Advisory in v1.0.0 per D6.

## Notes

Three CI-only failures were found and fixed during release:

1. Wrapper matrix job for Python did not install pytest.
2. check-cross-language did not install pytest.
3. release.sh jq expression double-escaped the status format.

Local gates passed throughout. The CI environment is a fresh
container per job; every dependency must be installed in the job
that uses it, not inherited from a shared requirements file.
