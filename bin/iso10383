#!/usr/bin/env bash
# Repo-local shim for the iso10383 CLI.
# The wrapper's console script will shadow this once it is installed.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
root="$(cd "$here/.." && pwd)"
exec python3 "$root/tools/iso10383_cli.py" "$@"
